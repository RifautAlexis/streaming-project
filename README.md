# StreamingProjectWorkspace

## Getting Started

`npx nx serve api-gateway`
`npx nx build api-gateway`

`npx nx serve frontend`
`npx nx build frontend`

## View inferred tasks
`nx show project project-name --web`